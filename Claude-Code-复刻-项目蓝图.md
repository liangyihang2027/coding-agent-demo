# 复刻 Claude Code 单 Agent · 工程内核学习蓝图（TypeScript 版）

> 目标：不是"再做一个能跑的 Agent"，而是**把 coding agent 的工程内核从零亲手实现一遍**，同时完成两件事：
> 1. **扬长**：巩固 AI 应用 / Agent 工程能力（Builder 招牌项目）
> 2. **补短**：通过亲手实现底层模块，补上简历缺失的「数据结构 / 算法 / 操作系统」深度，拿到面试通行证
>
> 一句话叙事：*"市面上的人都在用 coding agent，我想搞懂它内部怎么工作，于是把工程内核自己实现了一遍——模型只是其中一环。"*

---

## 0. 核心原则（先读，决定项目成败）

1. **带 ⭐ 的模块必须亲手实现，禁止调库**。它们是项目的灵魂，也是补短板的关键。
2. **价值不在功能多，在每一层都能讲出"为什么"**。面试官考的是深度。
3. **必须做到阶段 4（代码检索）**。做到阶段 3 就停 ≈ 白做，因为前几阶段没有区分度。
4. **每个阶段产出可测试、可演示、可写进 README 的成果**，并记录"踩过的坑 + 设计取舍"。

---

## 1. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | TypeScript (Node.js ≥ 20) | 贴合你前端全栈背景，叙事自然 |
| 运行形态 | CLI（命令行交互） | 接近 Claude Code 本体，聚焦内核而非 UI |
| 包管理 | pnpm | 快、依赖清晰 |
| LLM 接入 | OpenAI 兼容 SDK（流式） | 你已有流式 tool_calls 经验 |
| AST 解析 | `web-tree-sitter` / `tree-sitter` | 代码检索内核（阶段 4）|
| 子进程 | Node `child_process` 原生 | 沙箱内核，禁止用现成封装库 |
| 终端渲染 | `ink` 或原生 ANSI | 流式增量渲染 |
| 测试 | `vitest` | 给 Diff 引擎、检索做单测 |
| 类型校验 | `zod` | 工具参数 schema 校验 |

> 原则：**业务胶水可以用库，核心内核（Diff / 沙箱 / 检索 / 上下文管理）必须自己写。**

---

## 2. 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                        CLI 入口                           │
│              (终端交互 · 流式增量渲染)                      │
└───────────────────────────┬─────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────┐
│                     Agent Loop (Harness)                  │
│   ReAct 控制流 · step 上限 · 错误恢复 · 防死循环           │
└───┬───────────┬───────────┬───────────┬─────────────────┘
    │           │           │           │
┌───▼────┐ ┌───▼────┐ ┌────▼─────┐ ┌───▼──────────┐
│ LLM    │ │ 上下文 │ │  工具    │ │  权限/审批    │
│ 客户端 │ │ 管理 ⭐│ │ 注册表   │ │  闸门         │
│(流式)  │ │       │ │          │ │              │
└────────┘ └────────┘ └────┬─────┘ └──────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
   ┌────▼─────┐      ┌──────▼──────┐      ┌─────▼──────┐
   │ Diff/    │      │  沙箱/命令   │      │ 代码库检索 │
   │ Patch ⭐ │      │  执行 ⭐     │      │   ⭐       │
   └──────────┘      └─────────────┘      └────────────┘
```

模块依赖：上层只依赖下层接口，内核模块互相解耦，方便单测。

---

## 3. 内核模块清单（按工程深度 + 补短板价值排序）

| # | 模块 | 真正的难点 | 补到的能力 | 优先级 |
|---|---|---|---|---|
| 1 | Agent Loop / Harness | ReAct 控制流、step 上限、错误恢复、防死循环 | 状态机设计 | 必做 |
| 2 | Tool 协议层 | 流式 `tool_calls` 解析、并行调用、结果回填 | 协议/流处理 | 必做 |
| 3 | **上下文管理** ⭐ | 窗口超限时的压缩/截断/摘要、历史合并 | 算法 + 启发式 | 必做 |
| 4 | **代码库检索** ⭐ | 文件遍历、搜索、tree-sitter/AST、符号索引、相关性召回 | 数据结构 + 算法 | **灵魂** |
| 5 | **Diff / Patch 引擎** ⭐ | 可靠 `str_replace`/行级 apply、模糊匹配、冲突处理 | 字符串算法 | **灵魂** |
| 6 | **沙箱 / 命令执行** ⭐ | 子进程、超时 kill、流式输出、资源限制、危险命令拦截 | 操作系统/进程 | **灵魂** |
| 7 | 权限 / 审批系统 | 工具 gating、风险分级、人工确认闸门 | 系统设计/安全 | 必做 |
| 8 | Sub-agent / 任务委派 | 主拆任务、子 agent 隔离上下文、结果汇总 | 架构设计 | 加分 |
| 9 | 终端增量渲染 | 流式 UI、diff 高亮 | 前端性能 | 加分 |
| 10 | Token 计量 / checkpoint | 成本统计、会话快照与回滚 | 工程严谨度 | 加分 |

---

## 4. 分阶段里程碑

### 阶段 1 · 最小可用闭环（约 1 周）
**目标**：跑通一个最简 ReAct loop。

- [ ] LLM 客户端封装（流式响应）
- [ ] 流式 `tool_calls` 解析：用 `index`/`id` 双索引拼接分片，`finish_reason` 判断结束
- [ ] 工具注册表 + zod 参数校验
- [ ] 3 个基础工具：`read_file` / `write_file` / `run_command`（先不做沙箱）
- [ ] Agent Loop：思考 → 调工具 → 回填结果 → 循环

**面试得分点**：状态机设计、流式协议处理（你已有底子，快速过）。

---

### 阶段 2 · Diff / Patch 引擎 ⭐（约 1 周）
**目标**：实现可靠的文件编辑，不靠"整文件重写"。

- [ ] 实现 `str_replace`：在文件中精确替换一段文本
- [ ] 处理边界：匹配不唯一（报错并要求更多上下文）、空白/缩进差异、行尾差异
- [ ] 进阶：模糊匹配（容忍轻微空白差异）
- [ ] 行级 patch apply + 失败回滚
- [ ] **vitest 覆盖各种 edge case**（这是工程严谨度的证明）

**面试得分点**：字符串匹配算法、边界处理、"为什么不用整文件重写"（token 成本 + diff 可读性）。

**可深挖八股**：最长公共子序列、编辑距离、diff 算法（Myers）原理。

---

### 阶段 3 · 沙箱 / 命令执行 ⭐（约 1–1.5 周）
**目标**：安全地执行命令，这是纯正的 OS 能力。

- [ ] 用 `child_process.spawn` 封装命令执行
- [ ] 超时控制：到时 `kill`，处理子进程树（避免僵尸进程）
- [ ] stdout/stderr **流式捕获**（边执行边输出，不等结束）
- [ ] 工作目录隔离、环境变量控制
- [ ] 危险命令检测（`rm -rf`、`sudo`、网络请求等）+ 拦截
- [ ] 输出截断（超长输出不撑爆上下文）

**面试得分点**：进程管理、信号（SIGTERM/SIGKILL）、僵尸进程、IO 流、阻塞 vs 非阻塞。

**可深挖八股**：进程 vs 线程、fork/exec、管道、Node 事件循环与子进程的关系。

---

### 阶段 4 · 代码库检索 ⭐（约 1.5–2 周，最硬核）
**目标**：解决"大代码库塞不进 context window"这一核心难题。

- [ ] 文件遍历 + `.gitignore` 忽略规则解析
- [ ] 关键词 / 正则搜索（仿 ripgrep，可对比性能）
- [ ] **用 tree-sitter 做 AST 解析**：提取函数、类、符号定义
- [ ] 构建符号索引（符号名 → 位置的映射，考虑数据结构选型）
- [ ] **相关性召回**：根据 query 召回最相关的代码片段喂给模型
- [ ] 上下文裁剪：召回结果按 token 预算截断

**面试得分点**：数据结构（索引、Trie/倒排）、算法（相关性排序）、"大代码库怎么办"——这个问题答得好直接拉开差距。

**可深挖八股**：哈希表、前缀树、倒排索引、TF-IDF/BM25 思想、AST 是什么。

---

### 阶段 5 · 上下文管理 ⭐（约 1 周）
**目标**：上下文窗口超限时的智能处理。

- [ ] Token 计量（估算每条消息的 token）
- [ ] 超限策略：旧消息摘要压缩、工具结果裁剪、保留关键信息（如系统提示、近期对话）
- [ ] 历史合并 / 折叠
- [ ] 测试：构造超长对话，验证压缩后仍能正常工作

**面试得分点**：算法 + 启发式设计、LLM 应用核心难题、取舍能力（压缩 vs 信息损失）。

---

### 阶段 6 · 收尾与打磨（约 1 周）
- [ ] 权限 / 审批闸门（工具调用前风险分级 + 人工确认）
- [ ] Sub-agent 委派（可选加分）：主 agent 拆子任务，子 agent 隔离上下文
- [ ] 终端增量渲染（流式 UI + diff 高亮，复用你的 450ms 节流经验）
- [ ] Token 计量展示 + 会话 checkpoint / 回滚（可选）
- [ ] **README**：架构图 + 各模块设计取舍 + 踩坑复盘 + 一段 demo 录屏

**面试得分点**：能讲清"每个模块为什么这么设计"，系统设计的完整性。

---

## 5. 项目结构建议

```
claude-code-clone/
├── src/
│   ├── cli/                # 终端入口、交互、渲染
│   ├── agent/              # Agent Loop / Harness
│   │   ├── loop.ts
│   │   └── state.ts
│   ├── llm/                # LLM 客户端 + 流式 tool_calls 解析
│   ├── context/            # ⭐ 上下文管理（压缩/截断/计量）
│   ├── tools/              # 工具注册表 + 各工具实现
│   │   ├── registry.ts
│   │   ├── read-file.ts
│   │   ├── edit-file.ts    # 调用 diff 引擎
│   │   └── run-command.ts  # 调用沙箱
│   ├── diff/               # ⭐ Diff / Patch 引擎
│   ├── sandbox/            # ⭐ 沙箱 / 命令执行
│   ├── search/             # ⭐ 代码库检索（遍历/搜索/AST/索引）
│   ├── permission/         # 权限 / 审批闸门
│   └── types/              # 共享类型 + zod schema
├── tests/                  # vitest（重点覆盖 diff / search）
├── README.md               # 架构图 + 设计取舍 + 踩坑复盘
└── package.json
```

---

## 6. 面试反杀话术（做完后怎么讲）

| 面试官的刁难 | 你的反杀 |
|---|---|
| "你是不是只会调大模型？" | "我把 Claude Code 的工程内核从零实现了一遍，模型只是其中一环。" |
| 算法 / 数据结构 | "我做代码检索时用 tree-sitter 建符号索引、实现相关性召回……" |
| 操作系统 | "我实现沙箱时处理过子进程超时、僵尸进程、流式输出捕获……" |
| 字符串 / 算法 | "我的 patch 引擎处理了匹配不唯一和模糊匹配……" |
| 系统设计 | "我的工具层、权限层、内核层是解耦的，可以这样扩展……" |
| LLM 工程 | "上下文超限时我用摘要压缩 + 结果裁剪，权衡信息损失和 token 成本……" |

---

## 7. 推荐学习入口

- **tree-sitter**：官方文档 + `web-tree-sitter` 的 Node 用法；理解 AST、query 语法
- **子进程 / 进程管理**：Node `child_process` 官方文档；信号、进程树、僵尸进程概念
- **Diff 算法**：Myers diff 算法、最长公共子序列、编辑距离
- **检索 / 排序**：倒排索引、TF-IDF / BM25 的基本思想
- **参考实现（读源码，别抄）**：开源的 mini coding agent、Aider 的 repo map、Continue 的检索逻辑
- **LLM 工具协议**：OpenAI function calling / 流式 tool_calls 文档

---

## 8. 时间预估与里程碑检查点

| 阶段 | 周期 | 完成判定 |
|---|---|---|
| 1 最小闭环 | 1 周 | 能让 agent 读写文件、跑命令完成简单任务 |
| 2 Diff 引擎 ⭐ | 1 周 | 单测覆盖 ≥ 5 类编辑边界场景 |
| 3 沙箱 ⭐ | 1–1.5 周 | 超时/僵尸进程/流式输出/危险命令拦截都跑通 |
| 4 代码检索 ⭐ | 1.5–2 周 | 能在中型仓库召回相关代码喂给模型 |
| 5 上下文管理 ⭐ | 1 周 | 超长对话下压缩后仍正常工作 |
| 6 收尾打磨 | 1 周 | README + demo + 至少 1 个踩坑复盘 |

**总计约 6–8 周（认真做）。**

> ⚠️ 承诺要点：**必须做到阶段 4。** 阶段 1–2 没区分度，真正补深度、出差异化的是 4 / 3 / 5。做到阶段 3 就停约等于白做。

---

## 9. 给自己的一句话

> 你是 Builder——天性是"造出来就下一个"。这个项目最大的意义，是逼你**破天荒地"不为上线、只为搞懂"地把一个东西钻到底**。它既是面试通行证，也是你试探自己深度边界的一次修炼。
