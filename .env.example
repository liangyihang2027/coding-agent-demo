# 复制为 .env 并填入你的配置
# 任意 OpenAI 兼容服务都可以（OpenAI / DeepSeek / 通义 / 本地 vLLM 等）

# API Key
LLM_API_KEY=sk-xxxx

# OpenAI 兼容的 base url（OpenAI 官方留空即可，或填 https://api.openai.com/v1）
LLM_BASE_URL=https://api.openai.com/v1

# 使用的模型（需支持 function calling / tool_calls）
LLM_MODEL=gpt-4o-mini
