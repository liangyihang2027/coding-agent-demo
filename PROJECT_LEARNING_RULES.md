# 项目学习与开发规则

## 项目目标

这个项目的主要目的不是单纯堆功能，而是通过复刻和实现一个 agent 工程，系统理解真实 agent
工程的架构、落地细节和常见坑点。

希望通过这个项目提升这些能力：

- 理解 agent 工程的核心架构：LLM client、agent loop、tool registry、conversation state、CLI UI、权限审批、沙箱、上下文管理等模块如何协作。
- 学习工程落地时必须处理的细节：流式输出、工具调用顺序、错误回填、权限确认、用户交互、测试边界、模块文档和可维护性。
- 识别真实会遇到的坑：阻塞输入、事件语义混乱、工具执行风险、状态竞争、provider 差异、上下文膨胀、错误处理不完整等。
- 提升 agent 工程、服务端工程和程序员基础素养：模块边界、接口契约、安全默认值、可测试性、可观测性、代码 review 和复盘能力。

## 开发原则

每个模块都要服务于学习目标：既要能跑起来，也要说得清楚为什么这样设计、还有什么限制。

- 优先理解架构位置，再实现代码。
- 优先保持模块边界清晰，而不是把逻辑堆到一个文件里。
- 优先让行为可验证，而不是只靠手动感觉。
- 优先暴露真实问题，而不是用临时 hack 掩盖问题。
- 对安全、权限、文件系统、命令执行等能力保持保守默认值。

## 1. 执行前先澄清意图

在开始实现前，先确认任务目标、影响范围和验收标准。

- 如果需求有歧义，先提问，不要直接假设。
- 如果有多种实现路径，先说明关键取舍，再选择或等待确认。
- 如果改动会影响现有架构、数据、安全或用户体验，需要先指出风险。
- 如果任务涉及较大模块或架构变化，先写简短计划，再执行。

## 2. 写完代码后要验证、反思

每次完成代码改动后，都要做必要验证，并回看实现是否符合目标。

- 至少运行相关的类型检查、测试或最小可验证命令。
- 检查是否引入 lint、type、test 问题。
- 反思是否有遗漏边界、错误处理、权限、安全或回归风险。
- 总结实际改了什么、如何验证、还有什么已知限制。
- 如果发现新的坑点，把它记录到对应模块文档或项目文档中。

## 3. 每个文件夹都要有模块文档

每个功能目录都应有一份 Markdown 文档说明模块职责和实现原则。

- 文档应放在对应目录内，例如 `src/cli/CLI_UI.md`。
- 文档应说明模块边界、核心流程、关键类型、外部依赖和维护注意点。
- 模块有重要行为变更时，同步更新对应文档。
- 新增目录时，应同时补充该目录的模块文档。

## 推荐的模块开发节奏

开发每个模块时，按这个节奏推进：

1. 说明这个模块在 agent 架构里的位置。
2. 定义模块的输入、输出和边界。
3. 实现最小可用版本。
4. 补充关键路径测试或最小验证方式。
5. 记录实现取舍、踩坑点和后续改进方向。
6. 再考虑更复杂的工程化能力，例如取消、并发、缓存、日志、权限、沙箱、观测和回滚。

## Review 标准

代码 review 时优先看这些问题：

- 行为是否符合 agent 运行链路。
- 模块边界是否清晰，是否出现不必要的耦合。
- 错误是否能被模型、用户或调用方理解。
- 权限、安全和文件系统操作是否保守。
- 流式输出、工具调用、状态更新是否存在顺序问题。
- 是否有测试覆盖最容易回归的路径。
- 文档是否能帮助后续理解这个模块，而不是只描述表面代码。

